``` bash
python -m venv .venv
source .venv/bin/acrivate
pip install -r requirements.txt
```

``` bash
make init # install dependencies

make 1 # first task
make 2 # second task
make 3 # third task
```
